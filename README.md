# Java-Kiosk-Ordering-System
A simple console-based kiosk application built with Java that simulates a fast-food ordering system.  This program allows users to select menu items, enter quantities, and automatically calculates the total number of items ordered and the final cost at checkout.
